{
  "onSessionStart": [
    {
      "description": "Inject Suncode session context on Snow session start (additionalContext + .snow/log breadcrumb)",
      "hooks": [
        {
          "type": "command",
          "command": "python -X utf8 .snow/hooks/write-suncode-context.py session",
          "timeout": 20000,
          "enabled": true
        }
      ]
    }
  ]
}
